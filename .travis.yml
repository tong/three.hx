language: haxe

os:
  - linux

haxe:
  - "4.1.3"
  - development

matrix:
  allow_failures:
    - haxe: development

install:
  - haxelib dev three.hx .

script:
  - haxe haxedoc.hxml
  - haxe --cwd examples/basic build.hxml
